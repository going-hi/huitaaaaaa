# Дополнительный пароль перед интерфейсом phpMyAdmin (Apache Basic Auth).
<Directory /var/www/html>
    AuthType Basic
    AuthName "MindBase phpMyAdmin"
    AuthUserFile /etc/phpmyadmin-auth/.htpasswd
    Require valid-user
</Directory>
