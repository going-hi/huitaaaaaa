{
	email admin@mindbase-innim.ru
}

# Локальная разработка (без TLS)
http://localhost {
	reverse_proxy php:80
}

# Production: автоматический HTTPS (Let's Encrypt)
mindbase-innim.ru {
	reverse_proxy php:80
}

# phpMyAdmin по поддомену (нужна A-запись pma -> IP сервера)
pma.mindbase-innim.ru {
	reverse_proxy phpmyadmin:80
}
